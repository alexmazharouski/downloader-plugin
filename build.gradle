plugins {
    id 'java-gradle-plugin'
    id 'maven-publish'
    id 'com.gradle.plugin-publish' version '0.12.0'
}
group 'com.sample.gradle'
version '1.0-SNAPSHOT'
sourceCompatibility = 1.8

repositories {
    mavenCentral()
    mavenLocal()
}

gradlePlugin {
    plugins {
        downloader {
            id = 'com.sample.gradle'
            displayName = 'Downloader'
            description = 'Download files'
            implementationClass = 'com.sample.gradle.Downloader'
        }
    }
}

pluginBundle {
    website = 'https://github.com/alexmazharouski/downloader-plugin'
    vcsUrl = 'https://github.com/alexmazharouski/downloader-plugin.git'
    tags = ['download', 'template']
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}

